name: Build Frida 16.5.6 终极隐身版 (修复版)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 安装完整编译依赖
        run: |
          sudo dpkg --add-architecture i386
          sudo apt update
          sudo apt install -y \
            build-essential \
            lib32stdc++-12-dev \
            lib32z1-dev \
            libc6-dev-i386 \
            curl git zip sed \
            python3 python3-dev python3-pip \
            libglib2.0-dev \
            ninja-build \
            pkg-config \
            upx \
            openjdk-11-jdk \
            valac \
            clang \
            llvm-14 \
            gcc-multilib \
            g++-multilib \
            binutils

      - name: 修复 Python 头文件路径
        run: python3 -c "import sysconfig; print(sysconfig.get_paths())"

      - name: 克隆 Frida 16.5.6
        uses: actions/checkout@v4
        with:
          repository: frida/frida
          ref: 16.5.6
          submodules: recursive
          fetch-depth: 1
          path: frida

      - name: 克隆 Florida 16.5.6
        uses: actions/checkout@v4
        with:
          repository: Ylarod/Florida
          ref: 16.5.6
          submodules: recursive
          fetch-depth: 1
          path: florida

      - name: 应用 Florida 补丁
        run: |
          cd frida
          for file in $(cd ../florida && find . -type f); do
            mkdir -p $(dirname "$file")
            cp -f "../florida/$file" "$file" 2>/dev/null || true
          done

      - name: 移除 frida-python 避免编译冲突
        run: |
          cd frida
          sed -i 's/^\(.*subproject.*frida-python.*\)$/#\1/' meson.build || true

      - name: 【第一层】源码级混淆 - Hook 隐藏
        run: |
          cd frida
          
          echo "=== 源码级混淆开始 ==="
          
          # ========== 1. 重命名 Hook 结构体和函数 ==========
          GUM_INTERCEPTOR=$(find . -path "*/frida-gum/*" -name "guminterceptor.c" 2>/dev/null | head -1)
          if [ -f "$GUM_INTERCEPTOR" ]; then
            echo "修改: $GUM_INTERCEPTOR"
            sed -i 's/GumInterceptor/GumSystemMonitor/g' "$GUM_INTERCEPTOR"
            sed -i 's/gum_interceptor_attach/gum_system_monitor_register/g' "$GUM_INTERCEPTOR"
            sed -i 's/gum_interceptor_detach/gum_system_monitor_unregister/g' "$GUM_INTERCEPTOR"
            sed -i 's/attached_listeners/registered_callbacks/g' "$GUM_INTERCEPTOR"
            sed -i 's/listener_list/event_queue/g' "$GUM_INTERCEPTOR"
          fi
          
          # ========== 2. 全局字符串替换 ==========
          find . \( -name "*.c" -o -name "*.h" \) -type f -exec sed -i \
            -e 's/#define FRIDA_SERVER_PORT 27042/#define FRIDA_SERVER_PORT 59999/g' \
            -e 's/"frida"/"arm64v8"/g' \
            -e 's/"FRIDA"/"ARM64V8"/g' \
            -e 's/"frida-server"/"com.android.system.sync"/g' \
            -e 's/"frida-agent"/"libandroid_runtime"/g' \
            -e 's/"frida-jit"/"libhwui"/g' \
            -e 's/"gum-js-loop"/"EventDispatcher"/g' \
            -e 's/"frida-thread"/"System_Server"/g' \
            -e 's/"gmain"/"GCHandlerThread"/g' \
            -e 's/"gdbus"/"DBusServiceThread"/g' \
            {} \; 2>/dev/null || true
          
          # ========== 3. Vala 文件混淆 ==========
          find . -name "*.vala" -type f -exec sed -i \
            -e 's/"REJECT"/"ACCEPT"/g' \
            -e 's/"frida"/"system"/g' \
            -e 's/"FRIDA"/"SYSTEM"/g' \
            -e 's/"frida:rpc"/"android:ipc"/g' \
            -e 's/"frida:auth"/"android:auth"/g' \
            -e 's/"re\.frida"/"com\.android\.internal"/g' \
            {} \; 2>/dev/null || true
          
          # ========== 4. 隐藏关键函数 ==========
          find . \( -name "*.c" -o -name "*.h" -o -name "*.vala" \) -type f -exec sed -i \
            -e 's/on_function_enter/on_system_callback/g' \
            -e 's/on_function_leave/on_system_exit/g' \
            -e 's/gum_interceptor_/gum_system_monitor_/g' \
            {} \; 2>/dev/null || true
          
          echo "✓ 源码级混淆完成"

      - name: 【第二层】GUM 目录混淆
        run: |
          cd frida
          
          echo "=== GUM 层隐藏开始 ==="
          
          GUM_DIR=$(find . -type d -path "*/frida-gum/gum" 2>/dev/null | head -1)
          
          if [ -n "$GUM_DIR" ]; then
            echo "GUM 目录: $GUM_DIR"
            
            # 线程名隐藏
            find "$GUM_DIR" -name "gum-thread.c" -exec sed -i \
              -e 's/"gum-js-loop"/"EventDispatcher"/g' \
              -e 's/"frida-thread"/"System_Server"/g' \
              -e 's/"gmain"/"GCHandlerThread"/g' \
              -e 's/"gdbus"/"DBusServiceThread"/g' \
              {} \; 2>/dev/null || true
            
            # 库名伪装
            find "$GUM_DIR" -name "gum-allocator.c" -exec sed -i \
              -e 's/"frida-agent"/"libart"/g' \
              -e 's/"frida-jit"/"libhwui"/g' \
              {} \; 2>/dev/null || true
            
            # RWX 内存隐藏
            find "$GUM_DIR" -name "gummemory.c" -exec sed -i \
              -e 's/GUM_PAGE_READ_WRITE_EXEC/GUM_PAGE_READ_WRITE/g' \
              {} \; 2>/dev/null || true
            
            # ptrace 绕过
            find "$GUM_DIR" -name "gum-debug.c" -exec sed -i \
              -e 's/ptrace(/syscall(SYS_ptrace,/g' \
              {} \; 2>/dev/null || true
          fi
          
          # D-Bus 伪装
          CORE_DIR=$(find . -type d -path "*/frida-core" 2>/dev/null | head -1)
          if [ -n "$CORE_DIR" ]; then
            find "$CORE_DIR" -name "*.vala" -exec sed -i \
              -e 's/"frida"/"system"/g' \
              -e 's/"FRIDA"/"SYSTEM"/g' \
              -e 's/"re\.frida"/"android\.sys"/g' \
              {} \; 2>/dev/null || true
          fi
          
          echo "✓ GUM 层隐藏完成"

      - name: 【第三层】ARM64 代码混淆
        run: |
          cd frida
          
          echo "=== ARM64 混淆开始 ==="
          
          # 查找所有 ARM64 关键文件
          find . -path "*/frida-gum/*" -type f \( -name "*arm64*" -o -name "*aarch64*" \) -name "*.c" | while read file; do
            if [ -f "$file" ]; then
              echo "处理: $file"
              
              # 添加 NOP 垫片 (破坏 Hook 检测模式)
              sed -i 's/\(^[[:space:]]*\(mov\|ldr\|str\|b\|bl\)\)/    nop;\n\1/g' "$file" 2>/dev/null || true
            fi
          done
          
          echo "✓ ARM64 混淆完成"

      - name: 【第四层】编译准备 - 不修改 meson.build
        run: |
          cd frida
          
          echo "=== 编译环境准备 ==="
          
          # 创建自定义的编译脚本而不修改 meson.build
          cat > build-with-flags.sh << 'BUILDSCRIPT'
          #!/bin/bash
          
          # 设置深度混淆编译标志
          export CFLAGS="-O1 -g0 \
            -fno-inline \
            -fno-reorder-blocks \
            -fno-optimize-sibling-calls \
            -fno-asynchronous-unwind-tables \
            -fno-unwind-tables \
            -fvisibility=hidden \
            -ffunction-sections \
            -fdata-sections \
            -fno-common \
            -Wl,-z,relro,-z,now"
          
          export CXXFLAGS="$CFLAGS"
          export LDFLAGS="-Wl,--gc-sections,-z,relro,-z,now,--no-merge-exidx-entries,-fuse-ld=lld"
          
          echo "CFLAGS=$CFLAGS"
          echo "LDFLAGS=$LDFLAGS"
          
          # 继续正常编译
          make server-android-arm64
          BUILDSCRIPT
          
          chmod +x build-with-flags.sh
          echo "✓ 编译脚本准备完成"

      - name: 编译 Android arm64 (修复版)
        run: |
          cd frida
          
          echo "=== 开始编译 (深度混淆模式) ==="
          
          # 设置编译环境变量
          export CFLAGS="-O1 -g0 -fno-inline -fno-reorder-blocks -fno-optimize-sibling-calls -fvisibility=hidden -ffunction-sections -fdata-sections"
          export CXXFLAGS="$CFLAGS"
          export LDFLAGS="-Wl,--gc-sections,-z,relro,-z,now,--no-merge-exidx-entries"
          
          # 直接执行编译
          make server-android-arm64 2>&1 | tail -100 || {
            echo "编译失败，查看日志..."
            cat build/meson-logs/meson-log.txt 2>/dev/null | tail -50
            exit 1
          }
          
          echo "✓ 编译完成"

      - name: 【第五层】后编译二进制混淆
        run: |
          cd frida
          
          echo "=== 后编译混淆开始 ==="
          
          SERVER_BIN=$(find build -name "frida-server" -type f 2>/dev/null | head -1)
          if [ ! -f "$SERVER_BIN" ]; then
            echo "错误：未找到 frida-server"
            exit 1
          fi
          
          mkdir -p ../out
          cp "$SERVER_BIN" ../out/frida-server-final
          
          echo "处理二进制: ../out/frida-server-final"
          
          # ========== 1. 字符串深度混淆 ==========
          echo "【1】字符串替换..."
          sed -i 's/gum_interceptor/gum_system_mon/g' ../out/frida-server-final
          sed -i 's/gum_system_monitor_register/system_register/g' ../out/frida-server-final
          sed -i 's/frida:rpc/android:ipc/g' ../out/frida-server-final
          sed -i 's/frida:auth/android:auth/g' ../out/frida-server-final
          sed -i 's/"FRIDA"/"SYSD"/g' ../out/frida-server-final
          sed -i 's/frida/android/g' ../out/frida-server-final
          sed -i 's/27042/65535/g' ../out/frida-server-final
          sed -i 's/gmain/GCWorker/g' ../out/frida-server-final
          sed -i 's/gdbus/DBusWorker/g' ../out/frida-server-final
          sed -i 's/"listener"/"handler"/g' ../out/frida-server-final
          sed -i 's/"probe"/"event"/g' ../out/frida-server-final
          sed -i 's/"attach"/"bind"/g' ../out/frida-server-final
          
          # ========== 2. ELF 清理 ==========
          echo "【2】ELF 清理..."
          strip --strip-all ../out/frida-server-final 2>/dev/null || true
          strip -s --discard-all ../out/frida-server-final 2>/dev/null || true
          
          objcopy --strip-all \
                  --remove-section=.comment \
                  --remove-section=.note.gnu.build-id \
                  --remove-section=.note \
                  --remove-section=.gnu.version \
                  --remove-section=.gnu.version_r \
                  ../out/frida-server-final 2>/dev/null || true
          
          # ========== 3. ELF 头匿名化 ==========
          echo "【3】ELF 头匿名化..."
          python3 << 'PYSCRIPT'
          import struct
          
          with open("../out/frida-server-final", "r+b") as f:
              f.seek(7)
              f.write(b'\x00\x00')
          
          print("✓ ELF 头已匿名化")
          PYSCRIPT
          
          # ========== 4. UPX 压缩 ==========
          echo "【4】UPX 压缩..."
          upx --best --lzma ../out/frida-server-final 2>/dev/null || echo "UPX 可用时启用"
          
          # ========== 5. 随机填充 ==========
          echo "【5】随机填充..."
          python3 << 'PADDING'
          import os
          with open("../out/frida-server-final", "ab") as f:
              f.write(os.urandom(8192))
          print("✓ 填充完成")
          PADDING
          
          # ========== 6. 验证 ==========
          echo "【6】特征验证..."
          strings ../out/frida-server-final | grep -iE "frida|interceptor|listener|probe|27042" && echo "[!] 有残留" || echo "✓ 已隐藏"
          
          echo "✓ 后编译混淆完成"

      - name: 【第六层】生成反检测脚本
        run: |
          cat > out/anti-detect-ultimate.js << 'EOF'
          // ============================================
          // Frida 终极运行时对抗脚本 v4.0
          // 完全隐藏 Hook + 多层反检测
          // ============================================
          
          const CONFIG = {
              DEBUG: false,
              ENABLE_ALL: true
          };
          
          // ========== 1. 进程级伪装 ==========
          function hookProcessAPIs() {
              try {
                  var prctl = Module.findExportByName(null, "prctl");
                  if (prctl) {
                      Interceptor.attach(prctl, {
                          onEnter: function(args) {
                              if (args[0].toInt32() === 15) {  // PR_SET_NAME
                                  args[1] = Memory.allocUtf8String("surfaceflinger");
                              }
                          }
                      });
                  }
                  
                  ["getpid", "gettid"].forEach(function(fn) {
                      var func = Module.findExportByName(null, fn);
                      if (func) {
                          Interceptor.attach(func, {
                              onLeave: function(retval) {
                                  var pid = retval.toInt32();
                                  if (pid < 1000) retval.replace(1000 + (pid % 9000));
                              }
                          });
                      }
                  });
              } catch(e) {}
          }
          
          // ========== 2. 文件系统隐藏 ==========
          function hookFileSystemAPIs() {
              try {
                  var open = Module.findExportByName(null, "open");
                  var openat = Module.findExportByName(null, "openat");
                  
                  [open, openat].forEach(function(fn) {
                      if (fn) {
                          Interceptor.attach(fn, {
                              onEnter: function(args) {
                                  var path_ptr = openat ? args[1] : args[0];
                                  var path = path_ptr.readCString();
                                  if (path && path.match(/\/proc\/.*\/(maps|status|cmdline)/)) {
                                      this.block = true;
                                  }
                              },
                              onLeave: function(retval) {
                                  if (this.block) retval.replace(-1);
                              }
                          });
                      }
                  });
              } catch(e) {}
          }
          
          // ========== 3. 线程名隐藏 ==========
          function hookThreadAPIs() {
              try {
                  var pthread_setname_np = Module.findExportByName(null, "pthread_setname_np");
                  if (pthread_setname_np) {
                      Interceptor.attach(pthread_setname_np, {
                          onEnter: function(args) {
                              var name = args[1].readCString();
                              if (name && name.match(/gmain|gdbus|gum|frida/i)) {
                                  args[1] = Memory.allocUtf8String("Thread_" + Math.random().toString(16).slice(2, 8));
                              }
                          }
                      });
                  }
              } catch(e) {}
          }
          
          // ========== 4. 内存隐藏 ==========
          function hookMemoryAPIs() {
              try {
                  var mmap = Module.findExportByName(null, "mmap");
                  if (mmap) {
                      Interceptor.attach(mmap, {
                          onEnter: function(args) {
                              var prot = args[2].toInt32();
                              var RWX = 7;  // PROT_READ | WRITE | EXEC
                              if ((prot & RWX) === RWX) {
                                  args[2] = ptr(prot & ~4);  // 移除 EXEC
                              }
                          }
                      });
                  }
              } catch(e) {}
          }
          
          // ========== 5. Ptrace 防护 ==========
          function hookDebugAPIs() {
              try {
                  var ptrace = Module.findExportByName(null, "ptrace");
                  if (ptrace) {
                      Interceptor.attach(ptrace, {
                          onLeave: function(retval) {
                              retval.replace(-1);
                          }
                      });
                  }
              } catch(e) {}
          }
          
          // ========== 6. Java 层 ==========
          function hookJavaAPIs() {
              try {
                  Java.perform(function() {
                      var ClassLoader = Java.use("java.lang.ClassLoader");
                      ClassLoader.loadClass.overload('java.lang.String').implementation = function(className) {
                          if (className.match(/frida|gum|xposed/i)) {
                              throw new Error("Class not found");
                          }
                          return this.loadClass(className);
                      };
                      
                      var File = Java.use("java.io.File");
                      File.exists.implementation = function() {
                          var path = this.getAbsolutePath();
                          if (path && path.match(/frida|gum/i)) return false;
                          return this.exists();
                      };
                  });
              } catch(e) {}
          }
          
          // ========== 主入口 ==========
          function initialize() {
              console.log("[*] 启动运行时对抗系统...");
              
              hookProcessAPIs();
              hookFileSystemAPIs();
              hookThreadAPIs();
              hookMemoryAPIs();
              hookDebugAPIs();
              
              if (Java.available) {
                  hookJavaAPIs();
              }
              
              console.log("[✓] 运行时对抗系统已激活");
          }
          
          setTimeout(initialize, 1000);
          EOF
          
          echo "✓ 反检测脚本生成完成"

      - name: 【第七层】打包和验证
        run: |
          cd out
          
          echo "=== 最终验证 ==="
          echo "二进制大小: $(ls -lh frida-server-final | awk '{print $5}')"
          
          echo ""
          echo "=== 特征扫描 ==="
          strings frida-server-final 2>/dev/null | grep -iE "frida|gum|interceptor|listener|probe|27042" | head -5 && echo "[!] 有残留特征" || echo "✓ 完全隐藏"
          
          echo ""
          echo "=== 打包 ==="
          zip -q frida-ultimate.zip frida-server-final anti-detect-ultimate.js
          sha256sum frida-ultimate.zip > frida-ultimate.zip.sha256
          
          echo "✓ 打包完成"
          ls -lh frida-ultimate.zip*

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: frida-ultimate-build
          path: out/frida-ultimate.zip*
          retention-days: 30
